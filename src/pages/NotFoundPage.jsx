import React from "react";

const NotFoundPage = () => {
  return (
    <div
      style={{
        fontSize: "40px",
        fontWeight: "bold",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "red",
      }}
    >
      404 Not Found
    </div>
  );
};

export default NotFoundPage;
